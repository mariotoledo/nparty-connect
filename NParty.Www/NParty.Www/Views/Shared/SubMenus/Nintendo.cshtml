@model int
@{
    ViewBag.Title = "Nintendo";
}

<div class="content-area-menu">
    <nav class="navbar" style="min-height: initial; margin-bottom: 0px;">
        <div class="container">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#subMenu-Nintendo" aria-expanded="false">
                    <span class="sr-only">Toggle navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                <h3 style="margin-top: 12px; text-align: center;" class="hidden-lg">Nintendo</h3>
            </div>
            <div class="collapse navbar-collapse" id="subMenu-Nintendo">
                <ul class="nav nav-pills">
                    <li role="presentation" class="@(Model == 1 ? "active" : "")"><a href="#">Wii U</a></li>
                    <li role="presentation @(Model == 2 ? "active" : "")"><a href="#">3DS</a></li>
                    <li class="divider-vertical"></li>
                    <li role="presentation" class="@(Model == 3 ? "active" : "")"><a href="#">Home</a></li>
                    <li role="presentation" class="@(Model == 4 ? "active" : "")"><a href="#">Previews</a></li>
                    <li role="presentation" class="@(Model == 5 ? "active" : "")"><a href="#">Reviews</a></li>
                    <li role="presentation" class="@(Model == 6 ? "active" : "")"><a href="#">Retrô</a></li>
                    <li role="presentation" class="@(Model == 7 ? "active" : "")"><a href="#">Top 10</a></li>
                    <li class="divider-vertical"></li>
                    <li role="presentation" class="@(Model == 8 ? "active" : "")"><a href="#">Mario</a></li>
                    <li role="presentation" class="@(Model == 9 ? "active" : "")"><a href="#">Pokémon</a></li>
                    <li role="presentation" class="@(Model == 10 ? "active" : "")"><a href="#">The Legend of Zelda</a></li>
                </ul>
            </div>
        </div>
    </nav>
</div>
