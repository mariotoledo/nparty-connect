@model int
@{
    ViewBag.Title = "Eventos";
}

<div class="content-area-menu">
    <nav class="navbar" style="min-height: initial; margin-bottom: 0px;">
        <div class="container">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#subMenu-ESports" aria-expanded="false">
                    <span class="sr-only">Toggle navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                <h3 style="margin-top: 12px; text-align: center;" class="hidden-lg">Eventos</h3>
            </div>
            <div class="collapse navbar-collapse" id="subMenu-ESports">
                <ul class="nav nav-pills">
                    <li role="presentation" class="@(Model == 1 ? "active" : "")"><a href="#">Home</a></li>
                    <li class="divider-vertical"></li>
                    <li role="presentation" class="@(Model == 2 ? "active" : "")"><a href="#">Coberturas</a></li>
                    <li class="divider-vertical"></li>
                    <li role="presentation" class="@(Model == 3 ? "active" : "")"><a href="#">Anime Friends</a></li>
                    <li role="presentation" class="@(Model == 4 ? "active" : "")"><a href="#">Brasil Game Arena</a></li>
                    <li role="presentation" class="@(Model == 5 ? "active" : "")"><a href="#">BRasil Game Show</a></li>
                </ul>
            </div>
        </div>
    </nav>
</div>
